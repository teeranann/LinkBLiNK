# LinkBLiNK runtime dependencies. Developed against Python 3.12.
#
# scikit-learn is needed even though no module imports it directly: the
# random-forest judge is a pickled sklearn estimator that joblib cannot
# unpickle without it.

numpy>=1.24
pandas>=2.0
scipy>=1.10
opencv-python>=4.8
pillow>=10.0
matplotlib>=3.7
tqdm>=4.65
PyYAML>=6.0
joblib>=1.3
scikit-learn>=1.3
torch>=2.0
albumentations>=1.3
